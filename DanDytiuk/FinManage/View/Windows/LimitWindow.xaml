<Window x:Class="FinManage.View.Windows.LimitWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FinManage.View.Windows" xmlns:viewmodels="clr-namespace:FinManage.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:LimitWindowsViewModel}"
        xmlns:local_1="clr-namespace:FinManage.Models" xmlns:models="clr-namespace:FinManage.Models"
        mc:Ignorable="d"
        Title="LimitWindow" Height="578" Width="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="195*"></RowDefinition>
            <RowDefinition Height="185*"></RowDefinition>
            <RowDefinition Height="54*"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Button Grid.Column="0" Content="Save limit" Command="{Binding AddLimitCommand}" HorizontalAlignment="Center" Margin="0,10,0,0" Grid.Row="2" VerticalAlignment="Top" Width="247"/>
        <Button Grid.Column="1" Content="Delete limit" Command="{Binding DeleteLimitCommand}" HorizontalAlignment="Center" Margin="0,10,0,0" Grid.Row="2" VerticalAlignment="Top" Width="246"/>
        <Button Grid.Column="2" Content="Cancel" Command="{Binding CancelCommand}" HorizontalAlignment="Center" Margin="0,10,0,0" Grid.Row="2" VerticalAlignment="Top" Width="246"/>

        <DataGrid Grid.ColumnSpan="2" Grid.RowSpan="2" Grid.Column="1" ItemsSource="{Binding Limits}" SelectedItem="{Binding SelectedLimit, Mode=TwoWay}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn
                    Header="Category"
                    Binding="{Binding Category}" 
                    IsReadOnly="True"/>
                <DataGridTextColumn
                    Header="Amount"
                    Binding="{Binding Amount,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                    IsReadOnly="True"/>
                <DataGridTextColumn
                    Header="Currency" 
                    Binding="{Binding Currency, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    IsReadOnly="True"/>
                <DataGridTextColumn
                    Header="Description"        
                    Binding="{Binding Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    IsReadOnly="True"/>
                <DataGridTextColumn
                    Header="Start limit"
                    Binding="{Binding StartDate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, StringFormat=dd.MM.yyyy}"
                    IsReadOnly="True"/>
                <DataGridTextColumn
                    Header="End limit"
                    Binding="{Binding EndDate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, StringFormat=dd.MM.yyyy}"
                    IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <ComboBox HorizontalAlignment="Center" Width="120" RenderTransformOrigin="0.511,0.56" ItemsSource="{Binding CategoriesCB}" SelectedItem="{Binding SelectedCategory}" Margin="0,50,0,0" VerticalAlignment="Top"/>
        <TextBlock HorizontalAlignment="Center" Margin="0,20,0,0" TextWrapping="Wrap" Text="Category" VerticalAlignment="Top"/>

        <TextBlock HorizontalAlignment="Center" TextWrapping="Wrap" Text="Amount" VerticalAlignment="Top" Margin="0,95,0,0"/>
        <TextBox HorizontalAlignment="Center" Margin="0,126,0,0" TextWrapping="Wrap" Text="{Binding Amount, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, FallbackValue='0'}" VerticalAlignment="Top" Width="120" RenderTransformOrigin="0.506,0.082"/>

        <TextBlock HorizontalAlignment="Center" TextWrapping="Wrap" Text="Currency" VerticalAlignment="Top" Margin="0,169,0,0" Width="52" ></TextBlock>
        <ComboBox Margin="74,202,74,0" VerticalAlignment="Top" ItemsSource="{Binding CurrencyCB}" SelectedItem="{Binding Currency, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock HorizontalAlignment="Center" Margin="0,15,0,0" Grid.Row="1" TextWrapping="Wrap" Text="Description" VerticalAlignment="Top"/>
        <TextBox HorizontalAlignment="Center" Margin="0,49,0,0" Grid.Row="1" TextWrapping="Wrap" Text="{Binding Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Top" Width="120"/>

        <DatePicker HorizontalAlignment="Center" Margin="0,195,0,0" Grid.Row="1" VerticalAlignment="Top" Width="200" SelectedDate="{Binding EndDate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" DisplayDateStart="{Binding MinDate, Mode=OneWay}" DisplayDateEnd="{Binding MaxDate, Mode=OneWay}"/>
        <TextBlock HorizontalAlignment="Center" Margin="0,165,0,0" Grid.Row="1" TextWrapping="Wrap" Text="End limit date" VerticalAlignment="Top"/>

        <DatePicker HorizontalAlignment="Center" Grid.Row="1" VerticalAlignment="Top" Width="200" Margin="0,123,0,0" SelectedDate="{Binding StartDate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" DisplayDateStart="{Binding MinDate, Mode=OneWay}" DisplayDateEnd="{Binding MaxDate, Mode=OneWay}"/>
        <TextBlock HorizontalAlignment="Center" Margin="0,94,0,0" Grid.Row="1" TextWrapping="Wrap" Text="Start limit date" VerticalAlignment="Top" />

    </Grid>
</Window>
